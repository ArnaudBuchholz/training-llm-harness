# Coding agent workspace

This folder is the sandbox for the `coding` scenario.

All file operations performed by the coding agent (`read_folder`, `create_file`, `update_file`) are scoped to this directory. The agent cannot read or write anything outside it.

Feel free to add files here before starting a session, or let the agent create them from scratch.
